architecture: replication
replicaCount: 4  # 1 primary + 1 replica

primary:
  persistence:
    enabled: true
    size: 8Gi
#  extraFlags:
#    - "--require_secure_transport=ON"

secondary:
  persistence:
    enabled: true
    size: 8Gi
#  extraFlags:
#    - "--require_secure_transport=ON"

tls:
  enabled: true
  autoGenerated:
    enabled: true
    engine: helm  # Options: helm, cert-manager

service:
  type: ClusterIP
  port: 3306

primary:
  service:
    type: ClusterIP
    port: 3306
    annotations: {}
    labels: {}

secondary:
  service:
    enabled: false
