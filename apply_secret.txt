# 1. Create secrets (in default namespace) for MySQL
kubectl create secret generic mysql-credentials \
  --from-literal=root-password="$(openssl rand -base64 20)" \
  --from-literal=replication-password="$(openssl rand -base64 20)" \
  --from-literal=user-password="$(openssl rand -base64 20)"