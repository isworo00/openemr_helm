# 1. Create secrets (in default namespace) for MySQL
kubectl create secret generic mysql-credentials \
  --from-literal=mariadb-root-password="$(openssl rand -base64 20)" \
  --from-literal=mariadb-replication-password="$(openssl rand -base64 20)" \
  --from-literal=mariadb-password="$(openssl rand -base64 20)"