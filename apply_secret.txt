# Generate and apply Secret securely (no shell history leak)
kubectl create secret generic mysql-credentials --from-literal=mariadb-root-password="$(openssl rand -base64 20)" --dry-run=client -o yaml | kubectl apply -f -